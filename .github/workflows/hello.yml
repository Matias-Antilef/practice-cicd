name: Say hello
on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "Hola Shall!"
      - name: Dime la fecha
        run: date
      - name: Ver archivos del directorio actual
        run: ls -l
